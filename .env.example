# Server
PORT=3000

# Usage storage strategy: RAM | pocketbase
# When set to pocketbase, usage counters are persisted to pocketbase.
USAGE_STRATEGY=RAM

# Strategy selector: env | pocketbase | fetch
ENV_STRATEGY=env

# Strategy: env (direct key)
MISTRAL_API_KEY=your_mistral_api_key_here
# Optional: enable Gemini with env strategy
# GEMINI_API_KEY=your_gemini_api_key_here

# Optional: preferred key name to pick when multiple keys are returned
# Used by 'pocketbase' and 'fetch' strategies
# ENV_KEY_NAME=mistral

# Strategy: pocketbase
# PB_URL=https://your-pocketbase.example.com
# PB_USERNAME=admin@example.com
# PB_PASSWORD=your_password
# PB_USER_COLLECTION=users
# PB_KEYS_COLLECTIONS=keys
# When USAGE_STRATEGY=pocketbase, usage snapshots are saved here (defaults to "usage")
# PB_USAGE_COLLECTION=usage

# Strategy: fetch
# ENV_FETCH_URL=https://example.com/keys.json
# ENV_FETCH_TOKEN=token_if_required